<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:name=".MotoShopApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Project">

        <activity
            android:name=".ui.splash.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".ui.main.MainActivity"
            android:exported="false" />

        <activity
            android:name=".ui.inventory.AddEditMotorcycleActivity"
            android:exported="false" />

        <activity
            android:name=".ui.sales.CreateSaleActivity"
            android:exported="false" />

        <activity
            android:name=".ui.customer.CustomerDetailActivity"
            android:exported="false" />

        <activity
            android:name=".ui.repair.CreateRepairActivity"
            android:exported="false" />

        <activity
            android:name=".ui.supplier.ImportOrderActivity"
            android:exported="false" />

        <activity
            android:name=".ui.inventory.MotorcycleDetailActivity"
            android:exported="false" />

        <activity
            android:name=".ui.repair.RepairDetailActivity"
            android:exported="false" />

        <provider
            android:id="@+id/provider"
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.example.motoshop.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
